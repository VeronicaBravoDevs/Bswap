import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['res.cloudinary.com'], // Añade aquí los dominios de tus imágenes
  },
};

export default nextConfig;
